
    
    const selector = document.getElementById('packetSelector');
    const listPackets = document.querySelector('.list_packets');
    const allTara = document.querySelectorAll('.tara')
    const packets = document.querySelectorAll('.packet')

    packets.forEach(packet => {
        packet.addEventListener('click', () => {
            
        })
    })

